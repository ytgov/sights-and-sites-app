import React from 'react';
import {Text} from 'react-native';
import ScreenWrapper from '../../components/screenWrapper';

const HelpfulInfoScreen = () => {
    return (
        <ScreenWrapper>
            <Text>HelpfulInfoScreen</Text>
        </ScreenWrapper>
    );
};

export default HelpfulInfoScreen;
