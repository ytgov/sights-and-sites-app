import React from 'react';
import ScreenWrapper from '../../components/screenWrapper';
import {Text} from 'react-native';

const FilterByRegionScreen = () => {
    return (
        <ScreenWrapper>
            <Text>FilterByRegionScreen</Text>
        </ScreenWrapper>
    );
};

export default FilterByRegionScreen;
