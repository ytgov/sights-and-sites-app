import React from 'react';
import ScreenWrapper from '../../components/screenWrapper';
import {Text} from 'react-native';

const FilterIndexScreen = () => {
    return (
        <ScreenWrapper>
            <Text>FilterIndexScreen</Text>
        </ScreenWrapper>
    );
};

export default FilterIndexScreen;
