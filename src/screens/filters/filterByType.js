import React from 'react';
import ScreenWrapper from '../../components/screenWrapper';
import {Text} from 'react-native';

const FilterByTypeScreen = () => {
    return (
        <ScreenWrapper>
            <Text>FilterByTypeScreen</Text>
        </ScreenWrapper>
    );
};

export default FilterByTypeScreen;
