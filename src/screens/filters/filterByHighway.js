import React from 'react';
import ScreenWrapper from '../../components/screenWrapper';
import {Text} from 'react-native';

const FilterByHighwayScreen = () => {
    return (
        <ScreenWrapper>
            <Text>FilterByHighwayScreen</Text>
        </ScreenWrapper>
    );
};

export default FilterByHighwayScreen;
