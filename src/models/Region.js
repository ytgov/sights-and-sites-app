export default class Region {
    constructor(id = '', name = '', map = null, swoosh = null) {
        this.id = id
        this.name = name
        this.map = map
        this.swoosh = swoosh
    }
}
